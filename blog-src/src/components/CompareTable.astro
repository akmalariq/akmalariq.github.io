---
interface Props {
  caption?: string;
}

const { caption } = Astro.props;
---

<div class="figure">
  {caption && <p class="callout__title" style="margin-bottom:.75rem">{caption}</p>}
  <slot />
</div>
